const fs=require('node:fs'),p='components/district-designer.tsx';let s=fs.readFileSync(p,'utf8');s=s.replace('{MODELS,MODEL_MAP,ZONES','{MODELS,MODEL_MAP,RIVER,ZONES');s=s.replace("if(kind==='buildings'&&design.buildings.some(b=>overlaps(footprint(b),footprint(obj as Building))))continue;","if(kind==='buildings'&&(overlaps(footprint(obj as Building),RIVER)||design.buildings.some(b=>overlaps(footprint(b),footprint(obj as Building)))))continue;");fs.writeFileSync(p,s);
